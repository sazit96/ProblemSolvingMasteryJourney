#include<bits/stdc++.h>
using namespace std; 
int main ()
{
    int t; cin >> t; 
    while (t--)
    {
        int m; cin >> m; 
        for(int i=0;i<m;i++)
        {
            int n; cin >>n;
            vector<int>a(n);
            for(int j=0;j<n;j++)
            {
                cin >> a[i][j];
                
            }
        }
    }
    return 0;
}